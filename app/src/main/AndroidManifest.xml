<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_rental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ERental">
        <activity
            android:name=".login.sign_in"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MAIN / BERANDA -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- LOGIN / INTRO -->
        <activity
            android:name=".login.halamanku"
            android:exported="false" />
        <activity
            android:name=".login.halaman2"
            android:exported="false" />
        <activity
            android:name=".login.halaman3"
            android:exported="false" />
        <activity
            android:name=".login.signIn"
            android:exported="false" />
    </application>

</manifest>